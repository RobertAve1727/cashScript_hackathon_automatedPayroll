# Dependencies
node_modules/
package-lock.json
yarn.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Build
dist/
build/
# ...except the design system's assets, which are source, not build output.
# The bare `build/` rule above matches at any depth and was silently
# swallowing every stylesheet, font and icon the frontend needs.
!app/public/build/
*.log

# Local payroll data (JSON file persistence)
.payroll-data/

# Compiled contract artifacts are committed on purpose — see README
# ("Why artifacts are in git"). Do not add artifacts/ here.
